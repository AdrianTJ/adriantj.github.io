<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://adriantj.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://adriantj.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-04T20:14:26+00:00</updated><id>https://adriantj.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website of Adrian Tame Jacobo. Lead II Data Scientist @ S&amp;P Global. Mathematician, M.Sc. @ ITAM. Specializing in Bayesian optimization, time series forecasting, and causal inference. </subtitle><entry><title type="html">Mollify, Agents, and Integration</title><link href="https://adriantj.github.io/blog/2026/mollify/" rel="alternate" type="text/html" title="Mollify, Agents, and Integration"/><published>2026-07-08T00:00:00+00:00</published><updated>2026-07-08T00:00:00+00:00</updated><id>https://adriantj.github.io/blog/2026/mollify</id><content type="html" xml:base="https://adriantj.github.io/blog/2026/mollify/"><![CDATA[<p>For most of the last decade, Python tooling moved in one direction, and that direction was modular. The Unix philosophy won, small sharp tools proliferated, and answering a simple question about a codebase became an exercise in assembly. If you wanted to know what was unused you reached for <a href="https://github.com/jendrikseipp/vulture">vulture</a>. Lint was <a href="https://docs.astral.sh/ruff/">ruff</a>. Dependency hygiene was <a href="https://github.com/fpgmaas/deptry">deptry</a>, import boundaries were <a href="https://github.com/gauge-sh/tach">tach</a>, complexity was <a href="https://github.com/rubik/radon">radon</a>, duplication was <a href="https://github.com/kucherenko/jscpd">jscpd</a>, and security was <a href="https://github.com/PyCQA/bandit">bandit</a>. Each of these does its one job well, which is exactly why the arrangement felt like progress rather than fragmentation.</p> <p>The cost of that arrangement is easy to miss because it is not in any single tool. It is in the seams. Seven tools means seven configuration files, seven output formats, seven notions of severity, and no shared answer to the most basic question of all, which is what counts as a finding. A human absorbs that overhead without complaining, because a human is good at squinting at seven kinds of output and building one mental model out of them. The overhead only becomes visible when the reader is no longer a human.</p> <p>That is the argument I want to make here, and <a href="https://github.com/FavioVazquez">Favio</a> and I built <a href="https://github.com/FavioVazquez/mollify">Mollify</a> around it. Modular tool stacks are the right answer right up until the moment the job they are being asked to do gets harder than snapping modules together can satisfy. Clayton Christensen made this point about hardware and Ben Thompson has made it about software for years: when a product is not yet good enough for what is being asked of it, the integrated solution wins, because integration is what lets you optimize across the seams that modularity leaves exposed. Coding agents are the thing that just made the job harder, and the seams are where they fall down.</p> <h3 id="the-integrated-pass">The integrated pass</h3> <p>Mollify is a Rust engine that runs the equivalent of that whole stack in a single deterministic pass, with one configuration file and one output contract. In practice there is one command worth memorizing:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mollify audit <span class="nt">--path</span> /your/python/project
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">audit</code> runs every engine and prints a quality score from 0 to 100 on top of the findings:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mollify audit — /your/project
Quality score: 84/100
12 finding(s) across 47 file(s) — 0 error, 12 warn
  src/app.py:6   [warn/certain]  unused-export — function `_legacy` has no reachable references  (unused-export:931a82e6d41f07c3)
  src/api.py:88  [warn/likely]   high-complexity — function `handle` is complex (cyclomatic 14, cognitive 19)  (high-complexity:1aa9…)
  src/db.py:1    [warn/certain]  circular-dependency — import cycle: db → models → db  (circular-dependency:7c…)
  pyproject.toml:1 [warn/likely] unused-dependency — declared dependency `rich` is never imported  (unused-dependency:93…)
</code></pre></div></div> <p>Installing it is deliberately boring, which is part of the point, because every channel ships the same self-contained binary:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvx mollify audit              <span class="c"># one-off via uv, no install</span>
uv tool <span class="nb">install </span>mollify        <span class="c"># persistent, on your PATH</span>
pip <span class="nb">install </span>mollify            <span class="c"># or the pip route</span>
cargo <span class="nb">install </span>mollify-cli      <span class="c"># from crates.io, binary is `mollify`</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">audit</code> command is really a front end over a set of engines you can also run individually, and each maps to one of eight categories. The full surface is wider than this, but the shape of it is:</p> <table> <thead> <tr> <th>Area</th> <th>Command</th> <th>A sample of the rules</th> </tr> </thead> <tbody> <tr> <td>Dead code</td> <td><code class="language-plaintext highlighter-rouge">mollify dead-code</code></td> <td><code class="language-plaintext highlighter-rouge">unused-file</code>, <code class="language-plaintext highlighter-rouge">unused-export</code>, <code class="language-plaintext highlighter-rouge">unused-import</code>, <code class="language-plaintext highlighter-rouge">unused-method</code>, <code class="language-plaintext highlighter-rouge">unreachable-code</code>, <code class="language-plaintext highlighter-rouge">commented-code</code></td> </tr> <tr> <td>Dependency hygiene</td> <td><code class="language-plaintext highlighter-rouge">mollify deps</code></td> <td><code class="language-plaintext highlighter-rouge">unused-dependency</code>, <code class="language-plaintext highlighter-rouge">missing-dependency</code>, <code class="language-plaintext highlighter-rouge">transitive-dependency</code>, <code class="language-plaintext highlighter-rouge">misplaced-dev-dependency</code></td> </tr> <tr> <td>Architecture</td> <td><code class="language-plaintext highlighter-rouge">mollify arch</code></td> <td><code class="language-plaintext highlighter-rouge">circular-dependency</code>, <code class="language-plaintext highlighter-rouge">layer-violation</code>, <code class="language-plaintext highlighter-rouge">forbidden-import</code>, <code class="language-plaintext highlighter-rouge">private-import</code>, custom policies</td> </tr> <tr> <td>Complexity</td> <td><code class="language-plaintext highlighter-rouge">mollify complexity</code></td> <td><code class="language-plaintext highlighter-rouge">high-complexity</code>, <code class="language-plaintext highlighter-rouge">hotspot</code> (churn times complexity), <code class="language-plaintext highlighter-rouge">low-cohesion</code></td> </tr> <tr> <td>Duplication</td> <td><code class="language-plaintext highlighter-rouge">mollify dupes</code></td> <td><code class="language-plaintext highlighter-rouge">duplication</code>, via an exact suffix-array clone finder</td> </tr> <tr> <td>Type health</td> <td><code class="language-plaintext highlighter-rouge">mollify types</code></td> <td><code class="language-plaintext highlighter-rouge">untyped-function</code>, <code class="language-plaintext highlighter-rouge">private-type-leak</code></td> </tr> <tr> <td>Security</td> <td><code class="language-plaintext highlighter-rouge">mollify security</code></td> <td>eval, shell, SQL injection, weak crypto, secrets, each carrying a CWE id</td> </tr> </tbody> </table> <p>None of these individual capabilities is novel. vulture already finds dead code and bandit already finds security candidates. What is different is that they arrive as one evidence stream, ordered deterministically, under one contract, which turns out to matter far more than any single rule.</p> <h3 id="why-the-discipline-comes-first">Why the discipline comes first</h3> <p>There is a failure mode that integrated tools fall into, and it is worth naming because avoiding it is most of the work. When you unify eight kinds of analysis, you also unify eight opportunities to be confidently wrong, and a tool that cries wolf across eight categories at once is more annoying than seven honest tools that each cry wolf in their own corner. So the governing rule of the project is that no analysis, and specifically no AI, is allowed to invent a finding. Every result is a piece of deterministic evidence with a stable fingerprint, a confidence tier, and a reason a person can read.</p> <p>This is not modesty for its own sake. Dead-code detection in Python is undecidable in the general case, because <code class="language-plaintext highlighter-rouge">getattr</code>, <code class="language-plaintext highlighter-rouge">eval</code>, and <code class="language-plaintext highlighter-rouge">importlib</code> can conjure a reference out of a string at runtime, and pretending otherwise is precisely how static analyzers earn their reputation for noise. Mollify tiers every verdict instead of hiding the uncertainty. A finding marked <code class="language-plaintext highlighter-rouge">certain</code> is provable, a private symbol with no reachable reference and no dynamic dispatch in scope, and only those are ever auto-fixed. A <code class="language-plaintext highlighter-rouge">likely</code> finding is a strong static signal with some residual runtime risk. An <code class="language-plaintext highlighter-rouge">uncertain</code> finding sits on a public surface or near dynamic dispatch, and is reported for a human to judge, nothing more. The quality score then weights each finding by its confidence, so a report full of low-confidence review items does not get scored like one full of proven defects.</p> <p>The practical upshot is that you can act on the tool at whatever level of trust you want:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mollify audit <span class="nt">--min-confidence</span> likely   <span class="c"># drop the review-only candidates</span>
mollify fix                             <span class="c"># preview safe removals of certain dead code</span>
mollify fix <span class="nt">--apply</span>                     <span class="c"># actually write them</span>
</code></pre></div></div> <p>Framework awareness is part of the same discipline. Mollify understands Flask, FastAPI, Django, Celery, pytest, click, and Pydantic decorators, which means a route handler decorated with <code class="language-plaintext highlighter-rouge">@app.route</code> is not reported as dead just because nothing in the project calls it by name. That single piece of understanding removes the most common false positive that makes people turn dead-code tools off, and it is only possible because the analysis is integrated enough to reason about reachability and decorators together.</p> <h3 id="the-contract-is-the-product">The contract is the product</h3> <p>Here is where integration stops being a convenience and becomes the actual value. Every Mollify command emits the same <code class="language-plaintext highlighter-rouge">kind</code>-discriminated envelope, so a client, whether that is a shell script, a CI job, or an agent, switches on <code class="language-plaintext highlighter-rouge">kind</code> and walks <code class="language-plaintext highlighter-rouge">findings[]</code> without caring which engine produced them:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"kind"</span><span class="p">:</span><span class="w"> </span><span class="s2">"audit"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"schema_version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"0.1"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"quality_score"</span><span class="p">:</span><span class="w"> </span><span class="mi">84</span><span class="p">,</span><span class="w">
  </span><span class="nl">"summary"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"total"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"errors"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"warnings"</span><span class="p">:</span><span class="w"> </span><span class="mi">12</span><span class="p">,</span><span class="w"> </span><span class="nl">"files_analyzed"</span><span class="p">:</span><span class="w"> </span><span class="mi">47</span><span class="w"> </span><span class="p">},</span><span class="w">
  </span><span class="nl">"findings"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"fingerprint"</span><span class="p">:</span><span class="w"> </span><span class="s2">"unused-export:931a82e6d41f07c3"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"rule"</span><span class="p">:</span><span class="w"> </span><span class="s2">"unused-export"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"category"</span><span class="p">:</span><span class="w"> </span><span class="s2">"dead-code"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"severity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"warn"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"confidence"</span><span class="p">:</span><span class="w"> </span><span class="s2">"certain"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"reason"</span><span class="p">:</span><span class="w"> </span><span class="s2">"function `_legacy` has no reachable references in the project"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"location"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"path"</span><span class="p">:</span><span class="w"> </span><span class="s2">"src/app.py"</span><span class="p">,</span><span class="w"> </span><span class="nl">"line"</span><span class="p">:</span><span class="w"> </span><span class="mi">6</span><span class="p">,</span><span class="w"> </span><span class="nl">"end_line"</span><span class="p">:</span><span class="w"> </span><span class="mi">7</span><span class="w"> </span><span class="p">},</span><span class="w">
      </span><span class="nl">"actions"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
          </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"remove-symbol"</span><span class="p">,</span><span class="w">
          </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Delete unused function `_legacy`"</span><span class="p">,</span><span class="w">
          </span><span class="nl">"auto_fixable"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
          </span><span class="nl">"suppression_comment"</span><span class="p">:</span><span class="w"> </span><span class="s2">"# mollify: ignore[unused-export]"</span><span class="w">
        </span><span class="p">}</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Because the output is deterministic, identical input produces byte-identical output, which is the property that makes the contract worth building on. You can diff two reports, cache one, or hand one to a program and trust that nothing reordered itself between runs. A modular stack cannot offer this, not because any one tool is careless, but because there is no single artifact to make deterministic in the first place.</p> <p>Configuration follows the same logic of one file rather than seven. A <code class="language-plaintext highlighter-rouge">.mollifyrc.json</code> at the project root sets severities, thresholds, and architecture in one place:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"severity"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"dead-code"</span><span class="p">:</span><span class="w"> </span><span class="s2">"error"</span><span class="p">,</span><span class="w"> </span><span class="nl">"duplication"</span><span class="p">:</span><span class="w"> </span><span class="s2">"warn"</span><span class="p">,</span><span class="w"> </span><span class="nl">"unused-dependency"</span><span class="p">:</span><span class="w"> </span><span class="s2">"off"</span><span class="w"> </span><span class="p">},</span><span class="w">
  </span><span class="nl">"ignore"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"tests/"</span><span class="p">,</span><span class="w"> </span><span class="s2">"migrations/"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"max_cyclomatic"</span><span class="p">:</span><span class="w"> </span><span class="mi">10</span><span class="p">,</span><span class="w">
  </span><span class="nl">"architecture"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"layers"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"api"</span><span class="p">,</span><span class="w"> </span><span class="s2">"service"</span><span class="p">,</span><span class="w"> </span><span class="s2">"domain"</span><span class="p">,</span><span class="w"> </span><span class="s2">"infra"</span><span class="p">]</span><span class="w"> </span><span class="p">},</span><span class="w">
  </span><span class="nl">"policies"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"no-requests-in-domain"</span><span class="p">,</span><span class="w"> </span><span class="nl">"forbid_import"</span><span class="p">:</span><span class="w"> </span><span class="s2">"requests"</span><span class="p">,</span><span class="w"> </span><span class="nl">"in_paths"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"domain/"</span><span class="p">],</span><span class="w"> </span><span class="nl">"severity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"error"</span><span class="w"> </span><span class="p">}]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Raising a rule to <code class="language-plaintext highlighter-rouge">error</code> is what makes CI block, and the <code class="language-plaintext highlighter-rouge">policies</code> block lets you encode a rule like “the domain layer stays free of I/O” as a deterministic check rather than a comment someone leaves on a pull request and everyone forgets.</p> <p>CI is where the single contract earns back the most time. The flag that matters is <code class="language-plaintext highlighter-rouge">--gate new-only</code>, which attributes findings to changed files against a base branch and reports only what the current change introduced, so existing debt does not block every merge:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">mollify</span>
<span class="na">on</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">pull_request</span><span class="pi">]</span>
<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>
  <span class="na">security-events</span><span class="pi">:</span> <span class="s">write</span>
<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">mollify</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v5</span>
        <span class="na">with</span><span class="pi">:</span> <span class="pi">{</span> <span class="nv">fetch-depth</span><span class="pi">:</span> <span class="nv">0</span> <span class="pi">}</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">dtolnay/rust-toolchain@stable</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">cargo install mollify-cli</span>
      <span class="pi">-</span> <span class="na">run</span><span class="pi">:</span> <span class="s">mollify audit --gate new-only --base origin/$</span>
      <span class="pi">-</span> <span class="na">if</span><span class="pi">:</span> <span class="s">always()</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mollify audit --format sarif &gt; mollify.sarif</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">github/codeql-action/upload-sarif@v4</span>
        <span class="na">if</span><span class="pi">:</span> <span class="s">always()</span>
        <span class="na">with</span><span class="pi">:</span> <span class="pi">{</span> <span class="nv">sarif_file</span><span class="pi">:</span> <span class="nv">mollify.sarif</span> <span class="pi">}</span>
</code></pre></div></div> <p>If you would rather not lean on git for the diff, regression baselines do the same job off a content-derived snapshot, and because the fingerprints are derived from content rather than position, a baseline survives file moves and renames:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mollify audit <span class="nt">--save-baseline</span> .mollify/baseline.json                    <span class="c"># once, on a clean main</span>
mollify audit <span class="nt">--baseline</span> .mollify/baseline.json <span class="nt">--fail-on-regression</span>    <span class="c"># in CI</span>
</code></pre></div></div> <h3 id="the-new-reader">The new reader</h3> <p>Everything above would still be a reasonable argument if coding agents did not exist. It would be the familiar case that integration is a bit more convenient than assembly. What actually raises the stakes is that the reader has changed, and this is the part I find most interesting.</p> <p>A coding agent spends a remarkable share of its effort reconstructing facts that a static analyzer already knows for certain. Is this function used anywhere. What imports this module. Is this dependency actually needed. The agent answers those questions by grepping and inferring, and grep is exactly the wrong instrument, because it misses dynamic dispatch, re-exports, and framework wiring, and then acts on its own confident guess by deleting something that was load-bearing. The agent is not the problem. The problem is that it is being forced to rebuild ground truth from text every single time, and text is a lossy source.</p> <p>This is the demand that makes an integrated, deterministic tool valuable in a way it might not have been five years ago. An agent does not want to run seven tools and reconcile seven formats any more than you do, and it has far less patience for a source of truth that reorders itself between runs. It wants one call that returns structured, tiered evidence it can act on. So Mollify exposes exactly that, an MCP server, started with a single command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mollify mcp        <span class="c"># stdio MCP server for coding agents</span>
</code></pre></div></div> <p>You rarely run that yourself. The more useful entry point scaffolds version-matched skills, rules, hooks, and MCP configuration straight into a repository, in whatever form the agent expects:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mollify init <span class="nt">--agent</span> claude    <span class="c"># or cursor, gemini, codex, cascade</span>
mollify init <span class="nt">--all</span>             <span class="c"># every supported agent at once</span>
</code></pre></div></div> <p>For Claude Code that writes an <code class="language-plaintext highlighter-rouge">.mcp.json</code> registering the server as a tool, a <code class="language-plaintext highlighter-rouge">.claude/skills/mollify/</code> skill that teaches the agent the JSON contract and the meaning of the confidence tiers, slash commands under <code class="language-plaintext highlighter-rouge">.claude/commands/</code>, and hooks in <code class="language-plaintext highlighter-rouge">.claude/settings.json</code> so an audit can run automatically and block when your rules say <code class="language-plaintext highlighter-rouge">error</code>. Cursor gets <code class="language-plaintext highlighter-rouge">.cursor/rules/mollify.mdc</code> and its own <code class="language-plaintext highlighter-rouge">mcp.json</code>, Gemini gets <code class="language-plaintext highlighter-rouge">GEMINI.md</code> and <code class="language-plaintext highlighter-rouge">.gemini/settings.json</code>, Codex gets <code class="language-plaintext highlighter-rouge">AGENTS.md</code> and <code class="language-plaintext highlighter-rouge">.codex/config.toml</code>, and Devin and Cascade get their respective skills and workflows. The important detail is that these files are meant to be committed, which means every teammate’s agent reads the same deterministic ground truth rather than each one improvising its own from grep.</p> <p>The confidence tiers do real work in this setting too. Because only <code class="language-plaintext highlighter-rouge">certain</code> findings are auto-fixable, an agent with an auto-fix hook can remove provably dead code and leave the <code class="language-plaintext highlighter-rouge">likely</code> and <code class="language-plaintext highlighter-rouge">uncertain</code> candidates for a person, which is the correct division of labor between a machine that should never guess and a human who is allowed to.</p> <h3 id="the-wedge">The wedge</h3> <p>It is worth being precise about the claim, because the temptation is to overstate it. Mollify is not a better dead-code detector than vulture, and it is not trying to be. The individual tools each do their piece well and will keep doing so. The wedge is the unified deterministic pass under one contract, which is a different product category than any of them, and the reason that category is worth building now rather than in 2018 is that a new kind of consumer showed up who genuinely needs it.</p> <table> <thead> <tr> <th> </th> <th style="text-align: center">vulture</th> <th style="text-align: center">ruff</th> <th style="text-align: center">deptry</th> <th style="text-align: center">tach</th> <th style="text-align: center">radon</th> <th style="text-align: center">jscpd</th> <th style="text-align: center">bandit</th> <th style="text-align: center">Mollify</th> </tr> </thead> <tbody> <tr> <td>Whole-project dead code</td> <td style="text-align: center">yes</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> </tr> <tr> <td>Dependency hygiene</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> </tr> <tr> <td>Circular deps and boundaries</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> </tr> <tr> <td>Complexity and hotspots</td> <td style="text-align: center"> </td> <td style="text-align: center">~</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> </tr> <tr> <td>Duplication</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> </tr> <tr> <td>Security candidates with CWE</td> <td style="text-align: center"> </td> <td style="text-align: center">~</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> <td style="text-align: center">yes</td> </tr> <tr> <td>One deterministic pass and MCP contract</td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center"> </td> <td style="text-align: center">yes</td> </tr> </tbody> </table> <p>Mollify is early but real. It is published on <a href="https://pypi.org/project/mollify/">PyPI</a> and <a href="https://crates.io/crates/mollify-cli">crates.io</a>, it carries more than 210 tests, and it runs against itself in CI. Under the hood it is a Cargo workspace that flows from parse to graph to engines to report, built on Astral’s <code class="language-plaintext highlighter-rouge">ruff_python_parser</code> so the same distribution builds everywhere, with real LEGB scope resolution so a shadowing local never masks a dead top-level symbol, and a linear-time suffix array for exact duplication rather than hash-collision guessing. Those are the details that let the integrated pass stay honest.</p> <p>If you work on a Python codebase of any size, <code class="language-plaintext highlighter-rouge">uvx mollify audit</code> is a thirty-second look at what it finds. And if there is an agent anywhere in your workflow, <code class="language-plaintext highlighter-rouge">mollify init --agent</code> is the more interesting experiment, because it is a small bet on the same thesis the whole tool is built on, which is that the codebase should hand its truth to the machine instead of making the machine guess. The code, and the docs these examples came from, are at <a href="https://github.com/FavioVazquez/mollify">github.com/FavioVazquez/mollify</a>.</p>]]></content><author><name></name></author><category term="tools"/><category term="python"/><category term="rust"/><category term="static-analysis"/><category term="agents"/><category term="tooling"/><summary type="html"><![CDATA[why codebase intelligence for Python wants to be one integrated tool, and what changes now that the reader is a machine]]></summary></entry><entry><title type="html">three ways to run SQL locally</title><link href="https://adriantj.github.io/blog/2026/local_sql/" rel="alternate" type="text/html" title="three ways to run SQL locally"/><published>2026-01-07T00:00:00+00:00</published><updated>2026-01-07T00:00:00+00:00</updated><id>https://adriantj.github.io/blog/2026/local_sql</id><content type="html" xml:base="https://adriantj.github.io/blog/2026/local_sql/"><![CDATA[<p>When I was doing my masters in Data Science, one of the things I found frustrating was that we were not really taught how to interface with SQL databases and queries. This puts graduates at a disadvantage, because more and more it seems that SQL is the <em>lingua franca</em> of any large scale data analysis task.</p> <p>This article is a small group of solutions on how one could interface with SQL locally and without much hassle. I’ve found them to be quite useful, and enjoy exploring data this way.</p> <h3 id="solution-1-simon-willisons-solution-in-bash">Solution 1: Simon Willison’s solution in Bash</h3> <p>This is probably the easiest of the solutions to work with, since all it requires is sqlite3 to be installed for this to work. This solution comes from Simon Willison’s excellent blog, in particular, <a href="https://til.simonwillison.net/sqlite/one-line-csv-operations">this post</a>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sqlite3 :memory: <span class="nt">-cmd</span> <span class="s1">'.mode csv'</span> <span class="nt">-cmd</span> <span class="s1">'.import taxi.csv taxi'</span> <span class="nt">-cmd</span> <span class="s1">'.mode column'</span> <span class="se">\</span>
    <span class="s1">'SELECT passenger_count, COUNT(*), AVG(total_amount) FROM taxi GROUP BY passenger_count'</span>
</code></pre></div></div> <p>The script:</p> <ul> <li>Creates an in-memory SQLite database</li> <li>Imports taxi.csv as a table called taxi</li> <li>Runs an aggregate SQL query</li> <li>Prints the results in a readable, column-aligned format</li> </ul> <p>It’s a very Unix-y way to treat a CSV like a database table for fast exploration.</p> <h3 id="solution-2-duckdb-and-a-notebook-ui">Solution 2: DuckDB and a Notebook UI</h3> <p><a href="https://duckdb.org/install/?platform=macos&amp;environment=cli">Installing DuckDB</a> is simple enough, and it’s also fairly straightforward to use. DuckDB is a great and portable database solution, one of my favorites. It is particularly wonderful because it processes operations column-wise rather than row-wise. Per <a href="https://duckdb.org/why_duckdb">DuckDB’s page</a> on why to choose it:</p> <blockquote> <p>DuckDB uses a columnar-vectorized query execution engine, where queries are still interpreted, but a large batch of values (a “vector”) are processed in one operation. This greatly reduces overhead present in traditional systems such as PostgreSQL, MySQL or SQLite which process each row sequentially. Vectorized query execution leads to far better performance in OLAP queries.</p> </blockquote> <p>It is a very fancy-pants database-speak way of saying it is good at processing the types of queries traditionally associated with data analysis and data science tasks.</p> <p>DuckDB has an excellent <a href="https://duckdb.org/docs/current/clients/cli/overview">CLI client</a>, but the magic really is in their <a href="https://duckdb.org/2025/03/12/duckdb-ui">UI implementation</a>.</p> <p>Having worked with Snowflake, Databricks, BigQuery and the like before, an interface such as this one will be familiar to a lot of users, and it can provide a very solid jumping off point for users wanting to learn how to use these tools without having to commit to accounts with them or even having access to them through a large organization.</p> <p>The standard DuckDB UI looks like this:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_images/duckdb_ui-480.webp 480w,/assets/img/post_images/duckdb_ui-800.webp 800w,/assets/img/post_images/duckdb_ui-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_images/duckdb_ui.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>While, for reference, the Databricks one looks like this:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/post_images/dbx-notebook-demo-480.webp 480w,/assets/img/post_images/dbx-notebook-demo-800.webp 800w,/assets/img/post_images/dbx-notebook-demo-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/post_images/dbx-notebook-demo.gif" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>They’re obviously not 1:1, but if you wanted to learn to use the modern tools of the analytics world in a local environment without much hassle, this seems like a pretty nice path forward.</p> <h3 id="solution-3-jupyter-notebooks-with-sql-magics">Solution 3: Jupyter Notebooks with SQL Magics</h3> <p>If you are already working in a Jupyter environment (like JupyterLab or VS Code), you can run SQL queries directly inside your notebook cells using “magics.” This is incredibly useful for mixing data retrieval with Python analysis.</p> <p>The <code class="language-plaintext highlighter-rouge">jupysql</code> library is the modern standard for this. You can install it along with DuckDB:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>jupysql duckdb-engine
</code></pre></div></div> <p>Then, in your notebook, you can load the extension and connect to a local DuckDB instance (even an in-memory one):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">%</span><span class="n">load_ext</span> <span class="n">sql</span>
<span class="o">%</span><span class="n">sql</span> <span class="n">duckdb</span><span class="p">:</span><span class="o">///</span><span class="p">:</span><span class="n">memory</span><span class="p">:</span>
</code></pre></div></div> <p>Now you can write SQL directly in a cell by using the <code class="language-plaintext highlighter-rouge">%%sql</code> prefix:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">%%</span><span class="k">sql</span>
<span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="s1">'taxi.csv'</span> <span class="k">LIMIT</span> <span class="mi">5</span><span class="p">;</span>
</code></pre></div></div> <p>It’s the perfect bridge between raw SQL exploration and deeper Python-based data science, allowing you to stay in the flow of your analysis without switching tools.</p>]]></content><author><name></name></author><category term="SQL"/><category term="analytics"/><category term="tooling"/><summary type="html"><![CDATA[a small guide I wish I had when learning data science]]></summary></entry></feed>